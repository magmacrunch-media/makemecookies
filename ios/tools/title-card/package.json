{
  "name": "makemecookies-title-card-check",
  "version": "0.0.0",
  "private": true,
  "description": "Measures the built title card at every phone and iPad shape. Separate from ios/package.json so a bundle build does not install a browser.",
  "type": "module",
  "license": "SEE LICENSE IN ../../../LICENSE",
  "scripts": {
    "check": "node check.mjs"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
